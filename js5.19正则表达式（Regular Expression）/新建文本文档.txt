正则表达式（Regular Expression）：用来匹配字符串的（表单验证的主力）
Java和PHP也在用
工作原理：通配符匹配技术
比如：Windows操作系统里面的搜索


js的三大组成部分：ＥＣＭ
var reg=new RegExp(“\\d” “g”)//括号里面放的是匹配规则，匹配参数
量词+： 代表\d至少出现一次，最多无限次  例如：\d+

跟正则相关的方法：常用的有4个
1，字符串判断test

2，字符串搜索search（返回值是-1的话表示找不到位置）

失焦：
获焦：